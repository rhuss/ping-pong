apiVersion: v1
kind: Service
metadata:
  labels:
    name: pong
    project: ping-pong
    version: "1"
  name: pong
spec:
  selector:
    name: pong
    project: ping-pong
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30303
    name: http
  type: NodePort
