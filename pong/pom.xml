<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>jax2016</groupId>
  <artifactId>pong</artifactId>
  <version>1</version>
  <packaging>war</packaging>

  <name>pong</name>

  <properties>
    <!-- ARM alternative: hypriot/rpi-java -->
    <dmp.from>java:8-jre-alpine</dmp.from>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.wildfly.swarm</groupId>
        <artifactId>bom</artifactId>
        <version>1.0.0.Beta7</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <finalName>pong</finalName>

    <plugins>

      <plugin>
        <groupId>io.fabric8</groupId>
        <artifactId>docker-maven-plugin</artifactId>
        <version>0.14.2</version>
        <configuration>
          <images>
            <image>
              <name>rhuss/jax2016-pong</name>
              <build>
                <from>${dmp.from}</from>
                <cmd><shell>java -jar /maven/pong-swarm.jar</shell></cmd>
                <ports>
                  <port>8080</port>
                </ports>
                <env>
                  <PONG_STRENGTH>5</PONG_STRENGTH>
                </env>
                <assembly>
                  <inline>
                    <files>
                      <file>
                        <source>${project.build.directory}/pong-swarm.jar</source>
                      </file>
                    </files>
                  </inline>
                </assembly>
              </build>
              <!-- Don't run when called from parent -->
              <run><skip>true</skip></run>
            </image>
          </images>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.wildfly.swarm</groupId>
        <artifactId>wildfly-swarm-plugin</artifactId>
        <version>1.0.0.Beta7</version>
        <executions>
          <execution>
            <goals>
              <goal>package</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <version>2.6</version>
        <configuration>
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>

    <!-- Java EE 7 dependency -->
    <dependency>
      <groupId>javax</groupId>
      <artifactId>javaee-api</artifactId>
      <version>7.0</version>
      <scope>provided</scope>
    </dependency>

    <!-- Wildfly Swarm Fractions -->
    <dependency>
      <groupId>org.wildfly.swarm</groupId>
      <artifactId>jaxrs</artifactId>
    </dependency>

  </dependencies>
</project>
